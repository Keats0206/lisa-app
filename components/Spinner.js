import styles from "../styles/components/Spinner.module.scss"; // Component styles

// Spinner loading component for buttons
export default function Spinner() {
  return (
    <div className={styles.loading}></div>
  );
}
